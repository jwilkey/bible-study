{
  "name": "bible-study",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "new": "node scripts/new-study",
    "statics": "node scripts/generate-studies",
    "deploy": "npm run statics && gh-pages -d public -u 'Justin Wilkey <jwilkey@tentkeep.com>'",
    "start": "node scripts/serve",
    "serve": "npm run statics && nodemon node scripts/serve"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.16.0",
    "fs": "0.0.1-security",
    "gh-pages": "^2.1.1",
    "nodemon": "^2.0.3"
  }
}
