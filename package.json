{
  "name": "bible-study",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "new": "node scripts/new-study",
    "statics": "node scripts/generate-studies",
    "deploy": "npm run statics && gh-pages -d public -u 'Justin Wilkey <jwilkey@tentkeep.com>'",
    "start": "npm run statics && node scripts/serve",
    "serve": "nodemon --ignore public/ --exec npm start",
    "add": "node scripts/add-study"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^5.16.0",
    "fs": "0.0.1-security",
    "gh-pages": "^2.1.1",
    "nodemon": "^2.0.3"
  }
}
